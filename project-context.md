# Entry-Pass 프로젝트 컨텍스트 및 본사 개발 참고자료

> 작성 기준일: 2026-08-06  
> 대상 저장소: `dudo011/Entry-pass`  
> 기준 브랜치: `main`  
> 문서 목적: 자재센터 현장에서 제작한 출입신청 앱의 업무 요구사항, 구현 기능, 설계 판단 및 개선점을 본사 개발 담당자에게 전달하기 위한 참고자료

---

## 1. 문서의 성격

이 저장소는 자재센터 현장의 실제 업무 불편을 바탕으로 빠르게 제작하고 반복 개선한 **모바일 우선 프로토타입**이다.

본사 개발 시에는 다음 항목을 핵심 참고 대상으로 활용하는 것을 권장한다.

- 현장 사용자에게 필요한 업무 흐름
- 차량 유형별 안전수칙·동선·제출서류 규칙
- 차량기사·직원·관리자의 권한 구분
- 모바일 화면에서 반복 검증한 UX
- 비밀번호 분실, 차주 변경, 회원 삭제 등 실제 운영 중 발생하는 예외 처리
- 첨부 양식을 앱에서 직접 작성하고 PDF로 제출하는 방식

다만 현재 코드는 짧은 기간에 기능을 연속 추가하면서 여러 보완 스크립트와 Worker 버전 계층이 누적되어 있다. 따라서 **업무 요구사항과 UX는 적극 참고하되, 본사 공식 시스템의 장기 운영 구조로 소스를 그대로 복제하는 것은 권장하지 않는다.**

---

## 2. 해결하려는 현장 문제

기존 자재센터 출입 업무에서는 외부 차량기사가 현장에 도착한 뒤 구두로 출입을 요청하거나, 안전수칙과 작업서류를 현장에서 확인하는 경우가 많다.

이 과정에서 다음 문제가 발생한다.

1. 차량 도착 후 담당자를 찾고 승인받는 대기시간 발생
2. 차량 유형별 안전수칙과 이동 동선 안내가 일관되지 않음
3. 작업계획서·TBM·위험성 체크리스트 등 필수서류 누락
4. 승인·반려 과정과 사유가 체계적으로 남지 않음
5. 차량번호를 기준으로 한 기사 회원관리 및 차주 변경이 어려움
6. 비밀번호 분실 시 현장에서 수동으로 계정을 다시 만드는 문제
7. 출입 이력을 별도로 취합하여 관리하는 행정 부담

Entry-Pass는 이를 다음과 같이 개선하는 것을 목표로 한다.

> 차량기사가 방문 전에 모바일로 안전수칙을 확인하고 출입신청과 필요서류를 제출하면, 자재센터 직원이 미리 확인하여 승인 또는 반려하고, 승인된 차량은 안내된 동선으로 출입한다.

---

## 3. 서비스 핵심 개념

아파트 방문차량 사전등록과 유사한 구조이다.

```text
차량기사 사전 신청
  → 차량 유형별 안전수칙 확인
  → 차량 동선 확인
  → 신청정보 및 필수서류 제출
  → 자재센터 직원 검토
  → 승인 또는 반려
  → 차량기사 결과 확인
  → 승인 차량 출입
```

핵심 원칙은 다음과 같다.

- 출입 전에 안전수칙을 먼저 확인한다.
- 차량 유형에 따라 안내와 서류가 자동으로 달라진다.
- 모바일 한 화면에서 다음 행동이 명확해야 한다.
- 직원은 신청 건을 신속하게 승인·반려할 수 있어야 한다.
- 계정을 삭제하거나 차주를 변경해도 기존 출입기록은 보존한다.
- 작업 양식은 별도 PC 편집 없이 모바일에서 작성·저장할 수 있어야 한다.

---

## 4. 사용자와 권한 구조

### 4.1 차량기사

- 차량번호를 ID로 회원가입 및 로그인
- 이름, 연락처, 소속업체 등 기본정보 관리
- 차량 유형 선택
- 필수·기타 안전수칙 확인 및 동의
- 차량 유형별 동선 지도 확인
- 출입신청 정보 입력
- 필수 첨부서류 제출
- 본인 신청내역 및 승인·반려 결과 확인
- 비밀번호 분실 시 임시 비밀번호 발급 요청
- 임시 비밀번호 로그인 후 본인 비밀번호로 변경

### 4.2 자재센터 일반 직원

- 출입신청 목록 조회
- 신청 상세정보 및 첨부서류 확인
- 승인 또는 반려
- 회원관리 화면 사용
- 차량기사 회원 검색
- 임시 비밀번호 발급
- 비밀번호 발급 요청 승인·반려
- 차주 변경
- 회원 삭제 처리

### 4.3 관리자

일반 직원 권한을 모두 포함하며 다음 기능을 추가로 사용한다.

- 직원 가입신청 승인·반려
- 직원 사용중지·재개
- 직원 권한 변경
- 직원 계정 삭제
- 관리자 권한 부여 및 회수

### 4.4 현재 권한표

| 기능 | 차량기사 | 일반 직원 | 관리자 |
|---|---:|---:|---:|
| 출입신청 작성 | 가능 | 불가 | 불가 |
| 본인 신청 조회 | 가능 | 불가 | 불가 |
| 전체 신청 조회·승인·반려 | 불가 | 가능 | 가능 |
| 차량기사 회원관리 | 불가 | 가능 | 가능 |
| 비밀번호 요청 처리 | 불가 | 가능 | 가능 |
| 직원관리 | 불가 | 불가 | 가능 |

일반 직원도 화면 상단의 `관리자모드`로 진입할 수 있으나, `회원관리`만 활성화되고 `직원관리`는 잠금 상태로 표시된다.

관련 화면 구현: `public/js/admin-tools-menu-v6.js`

---

## 5. 차량 유형별 업무 규칙

차량 유형 설정의 기준 파일은 `data/vehicleTypes.js`이다.

현재 5개 유형을 운영한다.

| ID | 차량 유형 | 신청번호 접두어 | 주요 특징 |
|---|---|---:|---|
| `construction` | 공사업체 | A | 추가 안전수칙 및 작업서류 필수 |
| `transport` | 물자수송용역 차량 | B | 연간 물자수송 계약차량 |
| `delivery` | 기자재 납품차량 | C | 납품 목적 차량 |
| `scrap` | 불용품 매각차량 | D | 반출·계량 관련 동선, 작업서류 필수 |
| `pcbs` | PCBs처리용역 차량 | E | 위험물 관련 추가 안전수칙 및 작업서류 필수 |

### 5.1 공통 필수 안전수칙

- 안전모·안전화 등 안전장구 착용
- 운전위치 이탈 시 시동 정지, 파킹브레이크 및 고임목 조치
- 작업차량 작업반경 내 출입 금지
- 음주 후 현장 투입 금지
- 적재물 덮개 작업 시 안전대·안전고리 사용
- 혹한·혹서기 휴식 시행

### 5.2 공통 기타 안전수칙

- 자재센터 내 제한속도 준수
- 교차지점 일단정지
- 안전띠 착용
- 승차석 외 탑승 금지
- 운전면허 및 화물운송 자격 소지

### 5.3 공사업체·PCBs 추가 수칙

- 작업계획서 작성 및 TBM 시행
- 아웃트리거 등 전도방지 조치
- 인양물 고정장치 확인 및 하부 출입 금지

### 5.4 유형별 동선 지도

지도 파일은 외부 GitHub 원본주소가 아니라 앱의 정적 자산으로 제공한다.

```text
public/route-images/
├── construction.jpg
├── transport.jpg
├── scrap.jpg
└── pcbs.jpg
```

비공개 GitHub 저장소의 `raw.githubusercontent.com` 주소를 직접 사용할 경우 일반 사용자에게 이미지가 표시되지 않는 문제가 있었으므로, 본사 시스템에서도 지도·안내 이미지는 서비스 내부 자산 또는 승인된 저장소에서 제공해야 한다.

---

## 6. 차량기사 출입신청 흐름

현재 화면 흐름의 핵심은 다음과 같다.

1. 계약업체 로그인 또는 회원가입
2. 차량 유형 선택
3. 필수 안전수칙 확인
4. 기타 안전수칙 확인
5. 차량 동선 안내 확인
6. 출입신청 정보 입력
7. 필수서류 작성 또는 파일 첨부
8. 신청 제출
9. 신청번호 및 접수상태 확인
10. 직원 승인·반려 결과 확인

모바일 UX에서 중요하게 본 사항은 다음과 같다.

- 진행단계를 상단 막대로 표시
- `다음` 버튼을 화면 하단의 일정한 위치에 배치
- 화면마다 버튼 위치가 달라지지 않도록 통일
- 작업 중 불필요한 스크롤 최소화
- 중요한 문구와 목적지 위치를 크게 표시
- 차량기사에게 내부 시스템 용어보다 행동 중심 문구 사용

---

## 7. 첨부서류 및 앱 내 양식 작성

공사업체·불용품 매각·PCBs 처리 차량은 다음 서류를 제출한다.

| 키 | 표시명 | 필수 여부 | 작성 방식 |
|---|---|---:|---|
| `workPlan` | 작업계획서 | 필수 | 앱 내 3쪽 작성 후 PDF 저장 또는 파일 업로드 |
| `tbm` | TBM 회의록 | 필수 | 양식 위 필기 후 저장 또는 파일 업로드 |
| `safetyChecklist` | 위험성 체크리스트 | 필수 | 양식 위 필기 후 저장 또는 파일 업로드 |
| `sitePhoto` | 차량·운전자 현장사진 | 선택 | 현장 출입 후 사진 등록 |

### 7.1 작업계획서

최근 추가된 작업계획서는 3쪽으로 구성된다.

- 1쪽: 작업개요, 장비 사용계획
- 2쪽: 중량물 취급계획, 작업계획도
- 3쪽: 위험요인·안전대책, 작업지휘자·유도자 지정 및 교육

사용자는 페이지를 이동하면서 손가락으로 작성하고, 두 손가락으로 확대·이동할 수 있다.

- 되돌리기
- 현재 쪽 지우기
- 수정 후 재저장
- 3쪽을 `작업계획서.pdf` 한 파일로 생성
- 생성된 파일을 출입신청 첨부항목에 자동 연결

2쪽 작업계획도에는 실제 공사업체 차량 동선 지도를 사용하고 그 위에 작업 위치·경로·인원·장비를 표시할 수 있다.

관련 파일:

- `public/js/work-plan-form-editor.js`
- `public/js/work-plan-map-override.js`
- `public/js/work-plan-map-position-fix.js`

### 7.2 현재 파일 저장 방식

- 허용 형식: PDF, JPG, PNG
- 개별 파일 제한: 약 5MB
- 전체 multipart 요청 제한: 22MB
- 파일은 base64로 변환하여 D1 `documents` 테이블에 저장
- R2는 현재 사용하지 않음

이 방식은 초기 시범서비스에는 간단하지만, 본사 통합서비스에서는 문서 수와 용량 증가를 고려해 **오브젝트 스토리지 사용을 권장**한다.

---

## 8. 직원의 신청 검토 기능

직원 화면은 신청상태별 목록을 제공한다.

- 대기
- 승인
- 반려

주요 기능:

- 신청자 및 차량정보 확인
- 방문일시·방문목적 확인
- 차량 유형 확인
- 첨부서류 열람
- 승인 처리
- 반려 사유 입력
- 처리 이력 확인
- 당일 신청 중심 조회
- 신청 목록 Excel 내보내기

관련 프런트 파일 예시:

- `public/js/admin-detail-refined.js`
- `public/js/admin-today-only.js`
- `public/js/admin-excel-refined.js`
- `public/js/request-detail-visual-refined.js`

---

## 9. 차량기사 회원관리의 현장 업무 규칙

회원관리 기능은 단순 사용자 CRUD보다 현장 운영 규칙이 중요하다.

### 9.1 회원 검색

차량번호, 이름, 연락처, 소속업체를 대상으로 검색한다.

회원 카드에는 다음 정보를 표시한다.

- 차량번호와 이름
- 연락처
- 소속업체
- 새 비밀번호 설정 대기 상태
- 휴면 고객 상태

### 9.2 휴면 고객

다음 중 하나에 해당하면 `휴면 고객`으로 표시한다.

- 마지막 출입신청 이후 1년 이상 경과
- 출입신청 이력이 없고 가입 후 1년 이상 경과

휴면 표시는 현재 안내용이며 자동 삭제나 로그인 차단과 직접 연결되어 있지 않다.

### 9.3 임시 비밀번호 발급

직원이 직접 임시 비밀번호를 발급할 수 있다.

- 기존 세션 전부 종료
- 새 임시 비밀번호로 해시 교체
- `must_change_password = 1` 설정
- 기사에게 임시 비밀번호를 1회 표시
- 기사 로그인 후 새 비밀번호 설정 강제
- 처리 이력 기록

### 9.4 차주 변경

차량번호가 동일하고 운전자만 바뀌는 상황을 고려한 기능이다.

- 기존 차주 계정은 보관 상태로 변경
- 기존 차주의 출입신청 이력 유지
- 새 차주 계정을 동일 차량번호로 생성
- 새 차주에게 임시 비밀번호 발급
- 이전·신규 계정 모두 변경 이력 기록

### 9.5 회원 삭제

회원 삭제는 물리적 행 삭제가 아니라 **논리 삭제·보관 방식**이다.

- 로그인 ID에 삭제 식별값을 붙여 로그인 불가 처리
- 차량번호를 비워 동일 차량번호 재가입 허용
- 기존 세션 삭제
- 대기 중 비밀번호 요청 종료
- 기존 출입신청과 처리 이력 보존

관련 서버 파일: `src/driver-account-staff-api.js`

---

## 10. 비밀번호 분실 처리

차량기사 로그인 화면에 `비밀번호를 잊으셨나요?` 기능을 제공한다.

### 10.1 기사 요청정보

- 차량번호
- 이름
- 등록된 연락처

소속업체는 본인확인 입력항목에서 제외하였다.

### 10.2 처리 흐름

```text
기사 발급 요청
  → 입력정보와 회원정보 비교
  → 직원 회원관리 화면의 요청영역 표시
  → 직원 승인 또는 반려
  → 승인 시 임시 비밀번호 생성
  → 등록 연락처로 본인확인 후 직원이 안내
  → 기사 로그인
  → 새 비밀번호 설정
```

대기 요청이 0건일 때는 회원관리 화면의 발급 요청 박스를 숨기고, 1건 이상일 때만 표시한다.

SMS 자동발송은 현재 구현하지 않았다. 임시 비밀번호는 직원 화면에 한 번 표시하며, 직원이 등록 연락처로 본인확인 후 전화 등 승인된 방법으로 전달하는 구조이다.

관련 서버 파일: `src/password-reset-staff-api.js`

---

## 11. 직원 계정 및 권한관리

직원은 사번·이름·비밀번호로 가입을 신청하고 관리자가 승인한다.

주요 운영 규칙:

- 승인 전 직원 기능 접근 불가
- 관리자만 직원관리 가능
- 사용중지된 직원은 로그인 또는 관리기능 사용 불가
- 직원 권한 변경 시 기존 세션 종료 권장
- 마지막 관리자 1명은 권한 해제·삭제 방지 필요
- 공용 관리자 계정을 여러 사람이 함께 사용하지 않음

관련 데이터:

- `staff_applications`
- `staff_disabled`
- `users.staff_role`

---

## 12. 기술 구성

### 12.1 현재 스택

| 영역 | 기술 |
|---|---|
| 프런트엔드 | HTML, CSS, Vanilla JavaScript |
| 서버 | Cloudflare Workers |
| 라우팅·API 기반 | Hono 및 Worker 모듈 |
| 데이터베이스 | Cloudflare D1(SQLite 계열) |
| 첨부문서 | D1 base64 저장 |
| PWA | Web App Manifest + Service Worker |
| 저장소 | 비공개 GitHub 저장소 |
| 배포 | Wrangler |

### 12.2 실행·배포

```bash
npm install
npm run dev
npm run deploy
```

D1 초기화:

```bash
npm run db:init
npm run db:init:local
```

설정 파일: `wrangler.toml`

- Worker 진입점: `src/worker-v9.js`
- 정적 자산: `public/`
- D1 binding: `DB`
- D1 database name: `entry-pass-db`
- cron 설정: 매일 한국시간 오전 3시 기준

보안상 `database_id`, 계정정보, 토큰, 비밀번호 등 운영 비밀값은 본 문서에 기재하지 않는다.

---

## 13. 현재 코드 구조의 특징

현재 Worker는 기존 기능을 보존하면서 버전을 순차 확장하는 구조이다.

```text
worker-v9
  → worker-v8
    → worker-v7
      → 이전 Worker 구현
```

`worker-v9.js`는 일반 직원도 회원관리와 비밀번호 요청 처리를 할 수 있도록 다음 API를 우선 처리한 후 하위 Worker에 전달한다.

- `/api/admin/driver-accounts`
- `/api/admin/driver-accounts/...`
- `/api/admin/password-reset-requests`
- `/api/admin/password-reset-requests/...`

프런트엔드도 `public/index.html`에서 기본 `app.js` 다음에 여러 기능 보완 스크립트를 순차적으로 불러오는 구조이다.

이 구조는 기존 기능 회귀를 최소화하면서 빠르게 개선하기에는 유리했지만, 본사 재개발 시에는 다음과 같이 재구성하는 편이 좋다.

- 화면·상태·API 호출의 모듈화
- 차량유형·서류·안전수칙 설정을 중앙 설정으로 관리
- 인증·권한 검사를 공통 미들웨어로 통합
- DB 스키마 변경을 명시적 migration으로 관리
- UI 보완 스크립트를 하나의 화면 컴포넌트로 통합
- Worker 버전 체인을 단일 진입점으로 정리

---

## 14. 주요 데이터 구조

기본 스키마 파일: `schema.sql`

### 14.1 `users`

차량기사와 직원을 함께 저장한다.

주요 필드:

- `role`: `driver` 또는 `staff`
- `staff_role`: `admin` 또는 승인담당자
- `login_id`
- `name`
- `phone`
- `company`
- `default_vehicle_number`
- `default_vehicle_type_id`
- `must_change_password`
- 비밀번호 salt/hash

### 14.2 `sessions`

로그인 세션을 저장한다.

### 14.3 `requests`

출입신청의 핵심 정보와 승인상태를 저장한다.

- 신청번호
- 차량기사 계정
- 차량 유형
- 이름·연락처·차량번호·업체
- 방문목적·방문일시
- 안전수칙 동의
- 상태: `pending`, `approved`, `rejected`
- 반려사유
- 검토자·검토일시
- 이력 JSON
- 보존만료일

### 14.4 `documents`

신청별 첨부문서를 저장한다.

### 14.5 `staff_applications`

직원 가입신청을 저장한다.

### 14.6 `staff_disabled`

사용중지된 직원 계정을 관리한다.

### 14.7 `driver_account_events`

다음과 같은 차량기사 계정 관리 이력을 기록한다.

- 비밀번호 초기화
- 차주 변경
- 회원 삭제
- 비밀번호 요청 승인·반려

### 14.8 실행 중 추가 생성되는 테이블

현재 일부 확장 기능은 서버 시작 또는 최초 요청 시 필요한 테이블을 자동 생성한다.

- `password_reset_requests`
- `public_action_limits`
- `app_meta`

본사 버전에서는 모든 테이블을 migration에 명시하는 것이 좋다.

---

## 15. 현재 적용된 보안 기능

### 15.1 인증·세션

- PBKDF2-SHA256 비밀번호 해시
- 반복 횟수 100,000회
- 랜덤 salt
- `__Host-ep_session` 세션 쿠키
- `Secure`, `HttpOnly`, `SameSite=Strict`
- CSRF 전용 쿠키 및 헤더 확인
- 직원 세션 약 48시간
- 차량기사 세션 약 30일
- 비밀번호 초기화·회원 삭제·차주 변경 시 기존 세션 폐기

### 15.2 요청 보호

- 변경 요청의 Origin 확인
- `Sec-Fetch-Site: cross-site` 차단
- URL 쿼리 인증토큰 차단
- JSON 최대 64KB
- multipart 전체 최대 22MB
- 공개 회원가입·직원가입·비밀번호 요청의 IP 기준 횟수 제한

### 15.3 응답 보안 헤더

- HSTS
- CSP
- 클릭재킹 차단
- MIME sniffing 차단
- Referrer 제한
- 카메라·마이크·위치·결제·USB 권한 차단
- API 및 첨부파일 no-store

### 15.4 PWA 캐시 정책

Service Worker는 최신 코드 반영을 우선하여 정적 파일을 장기간 캐시하지 않고 `cache: no-store`로 네트워크에서 다시 불러온다.

관련 파일: `public/sw.js`

---

## 16. 배포 전 또는 본사 개발에서 반드시 재검토할 항목

### 16.1 현재 미확정 사항

- 비밀번호 최소 자릿수와 복잡도
- 회사 공식 도메인 연결
- 회사 계정 기반 SSO 또는 직원 본인확인 방식
- 관리자 다중인증
- 개인정보 보유·파기기간
- 첨부문서 악성코드 검사
- 외부 클라우드 사용 및 개인정보 국외처리 승인
- D1 백업·복구 책임과 보관장소
- 장애 대응 및 운영 담당자
- 알림수단: 전화, SMS, 사내 알림, 카카오 알림톡 등

### 16.2 기술적 개선 필요사항

- D1에 base64 문서를 저장하는 구조의 확장성
- CSP의 `style-src 'unsafe-inline'`
- 프런트 보완 스크립트 누적
- Worker 버전 체인 누적
- 공식 DB migration 부재
- 자동화된 단위·통합·E2E 테스트 부재
- GitHub Actions 기반 CI 미구성
- 정식 취약점 진단 및 모의해킹 미실시

### 16.3 보존정책 정합성

현재 `wrangler.toml`에는 매일 실행되는 cron이 설정되어 있으나, `DEPLOY.md`에는 신청과 문서를 자동 삭제하지 않는다고 적혀 있다.

본사 개발 또는 정식 운영 전 다음을 하나로 확정해야 한다.

- 법정·업무상 보존기간
- 보존 만료 후 자동삭제 여부
- 논리삭제 또는 물리삭제 방식
- 백업본의 보존기간
- 사용자 탈퇴 시 개인정보와 출입기록의 분리 처리

현재 문서와 코드의 보존정책은 정합성 재확인이 필요하다.

---

## 17. 현장 테스트에서 확인된 UX 교훈

1. **권한 확인 후 버튼을 나중에 추가하면 화면이 깜박인다.**  
   직원·관리자 역할을 초기 화면 렌더링 전에 반영해야 한다.

2. **설치형 PWA는 이전 JavaScript가 남아 보일 수 있다.**  
   캐시 버전과 Service Worker 갱신전략이 중요하다.

3. **비공개 GitHub의 raw 이미지 링크는 사용자 화면에서 실패한다.**  
   지도와 양식 이미지는 앱 내부 자산으로 제공해야 한다.

4. **모바일에서는 버튼 위치와 터치영역이 기능만큼 중요하다.**  
   최소 44~50px 터치영역과 화면 간 동일한 버튼 위치가 필요하다.

5. **관리자와 일반 직원의 기능 차이는 화면과 서버에서 모두 검사해야 한다.**  
   메뉴만 숨기는 것으로는 부족하다.

6. **회원 삭제는 기록 삭제와 분리해야 한다.**  
   출입기록을 보존하면서 로그인만 막는 방식이 현장에 적합하다.

7. **차량번호 중심 계정은 차주 변경을 별도 업무로 다뤄야 한다.**  
   기존 기사정보를 덮어쓰면 과거 출입기록의 주체가 바뀌는 문제가 생긴다.

8. **첨부 양식을 앱에서 직접 작성할 수 있으면 현장 사용성이 크게 향상된다.**  
   다만 모바일 손글씨만으로 모든 문서 입력을 해결하기보다, 본사 버전에서는 구조화된 입력필드와 전자서명도 검토할 필요가 있다.

---

## 18. 본사 개발 시 우선 가져갈 요구사항

### 필수 유지 권고

- 차량 유형별 안전수칙·동선·필수서류 자동 분기
- 출입 전 안전수칙 확인과 동의
- 모바일 중심 신청 흐름
- 승인·반려 및 반려사유
- 첨부서류 미리보기
- 작업계획서·TBM·위험성 체크리스트 작성 지원
- 차량번호 기반 회원관리
- 차주 변경 시 기존 이력 보존
- 회원 삭제 시 기존 신청기록 보존
- 비밀번호 발급 요청과 직원 처리
- 일반 직원의 회원관리 권한
- 관리자 전용 직원관리
- 감사 가능한 계정관리 이력
- 신청목록 Excel 내보내기 또는 본사 시스템 연계

### 본사 공통플랫폼으로 확장할 때 검토

- 사업소·자재센터별 안전수칙 설정
- 사업소별 동선 지도와 정차위치 설정
- 차량 유형 및 제출서류 관리자 설정화
- 본사 통합 직원 SSO
- 사업소별 권한과 데이터 범위
- 중앙 통계 및 대시보드
- ERP·출입통제·QR 게이트 연계
- 알림 자동화
- 문서 위변조 방지 및 전자서명
- 개인정보 마스킹 및 다운로드 통제
- 감사로그 중앙 수집

---

## 19. 본사 담당자와 협의할 핵심 질문

1. 본사에서 개발 중인 앱의 대상 범위는 특정 자재센터인가, 전사 공통인가?
2. 차량 유형과 안전수칙을 사업소별로 다르게 설정할 수 있는가?
3. 직원 로그인은 사내 SSO와 연계할 수 있는가?
4. 외부 차량기사의 신원확인 수준은 어느 정도로 할 것인가?
5. 차량번호를 계정 ID로 사용하는 방식을 유지할 것인가?
6. 차주 변경 시 기존 출입이력을 어떤 기준으로 보존할 것인가?
7. 작업계획서 등 양식을 전자문서로 구조화할 것인가, PDF 첨부로 유지할 것인가?
8. 첨부파일 저장소와 악성코드 검사는 어떤 인프라를 사용할 것인가?
9. 승인·반려 알림은 어떤 수단으로 보낼 것인가?
10. 출입기록과 개인정보의 보유·파기기간은 얼마인가?
11. 관리자·일반 직원·승인담당자의 역할을 어떻게 구분할 것인가?
12. 사업소별 데이터는 서로 분리할 것인가, 본사에서 통합 조회할 것인가?
13. 장애 시 수기 출입처리 절차와 복구 목표시간은 무엇인가?
14. 현장 프로토타입의 기능 중 본사 앱에 즉시 반영 가능한 항목은 무엇인가?

---

## 20. 주요 파일 안내

| 파일 | 역할 |
|---|---|
| `data/vehicleTypes.js` | 차량 유형, 안전수칙, 동선, 필수서류 설정 |
| `public/js/app.js` | 기본 화면 흐름과 상태관리 |
| `public/index.html` | CSS·JavaScript 로드 순서 |
| `src/worker-v9.js` | 현재 Worker 진입점 |
| `src/security-hardening.js` | 공통 보안검사와 보안헤더 |
| `src/driver-account-staff-api.js` | 직원용 차량기사 회원관리 API |
| `src/password-reset-staff-api.js` | 직원용 비밀번호 요청 처리 API |
| `schema.sql` | 기본 D1 스키마 |
| `public/js/admin-tools-menu-v6.js` | 회원관리·직원관리 메뉴 및 역할 분기 |
| `public/js/driver-account-management-v2.js` | 차량기사 회원관리 UI |
| `public/js/password-reset-flow-v2.js` | 비밀번호 요청 UI |
| `public/js/work-plan-form-editor.js` | 3쪽 작업계획서 작성 및 PDF 생성 |
| `public/js/work-plan-map-override.js` | 작업계획도에 실제 동선 지도 삽입 |
| `public/route-images/` | 차량 유형별 동선 지도 |
| `public/sw.js` | 최신 코드 우선 PWA 캐시 정책 |
| `wrangler.toml` | Cloudflare Workers·D1·cron 설정 |
| `DEPLOY.md` | 현재 배포 절차 |

---

## 21. 현재 검증 수준

지금까지 기능별로 다음과 같은 검증을 수행했다.

- Android 모바일 실사용 화면 확인
- Chromium 모바일 에뮬레이션
- JavaScript 구문 검사
- 주요 API의 로컬 D1 모의 테스트
- 권한별 접근 테스트
- CSRF 및 공개 요청 횟수 제한 테스트
- 임시 비밀번호 발급·세션종료 테스트
- 차주 변경·회원 삭제 시 기록보존 테스트
- 작업계획서 PDF 생성 및 파일용량 확인

현재 GitHub Actions CI는 설정되어 있지 않으며, 정식 운영 전에는 다음 자동화가 필요하다.

- 단위 테스트
- API 통합 테스트
- 권한별 E2E 테스트
- 데이터 migration 테스트
- 보안 정적분석
- 의존성 취약점 검사
- 배포 후 smoke test

---

## 22. 본사 담당자에게 전달할 요약

Entry-Pass의 가장 중요한 성과는 기술 자체보다 **현장에서 필요한 출입 업무의 세부 규칙과 모바일 사용 흐름을 실제 화면으로 검증했다는 점**이다.

본사 개발 시 다음 세 가지를 우선 참고하기 바란다.

1. 차량 유형별 안전수칙·동선·제출서류가 자동으로 달라지는 구조
2. 차량번호 기반 회원관리에서 발생하는 비밀번호 분실·차주 변경·회원 삭제의 업무 규칙
3. 작업계획서·TBM·위험성 체크리스트를 모바일에서 직접 작성하고 제출하는 경험

공식 시스템은 본사 표준 인증, 개인정보 정책, 문서저장소, 감사로그, 백업·복구, 사업소별 설정 기능을 포함해 재설계하는 것이 적절하다. 현장 프로토타입은 해당 시스템의 요구사항 검증본이자 사용자 경험 참고자료로 활용할 수 있다.

---

## 23. 변경 및 유지관리 원칙

- 차량 유형·안전수칙·제출서류 변경은 우선 `data/vehicleTypes.js`를 확인한다.
- 권한 변경은 UI와 서버 API를 함께 수정한다.
- 기존 신청기록이 삭제되는 변경은 금지한다.
- DB 구조 변경 전 백업과 migration 계획을 마련한다.
- 지도·양식 이미지는 앱 내부 자산으로 제공한다.
- 배포 전 Android 모바일 화면과 권한별 흐름을 확인한다.
- 운영 비밀값은 저장소 문서에 기록하지 않는다.

본 문서는 기능 변경 시 함께 갱신하는 것을 권장한다.

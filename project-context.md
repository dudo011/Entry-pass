# Entry-Pass 프로젝트 컨텍스트

> 최종 갱신일: 2026-08-09  
> 저장소: `dudo011/Entry-pass`  
> 기준 브랜치: `main`  
> 현재 Worker 진입점: `src/worker.js`  
> 문서 목적: 현재 구현 구조와 현장 업무 규칙을 다음 개발자 또는 AI 도구에 정확하게 인계하기 위한 문서. 특히 **계약업체 기능**과 **자재센터 직원·관리자 기능**을 서로 혼동하지 않도록 영역을 분리해 설명한다.

---

## 1. 프로젝트 성격

Entry-Pass는 자재센터 외부 차량 출입을 사전에 신청·승인하고, 차량기사에게 안전수칙과 이동동선을 안내한 뒤 현장사진까지 확인하여 출입 절차를 완료하는 **모바일 우선 PWA 프로토타입**이다.

현재 기술 구성:

- HTML / CSS / Vanilla JavaScript 중심 SPA
- Cloudflare Workers + D1
- Worker는 과거 `worker.js + worker-v2..v10` 체인을 `src/worker.js` 하나로 병합했지만 내부 레이어 동작 순서는 보존
- 기본 화면 이후 여러 보정 JavaScript를 순차 로드하는 구조
- Android/Galaxy 실제 화면을 기준으로 UX를 반복 조정
- 현장 요구사항 검증용 프로토타입 성격이 강함

개발 시 **검증된 업무 흐름과 UX는 유지하되, 누적된 보정 스크립트를 무분별하게 추가하지 않는 것**이 중요하다.

---

## 2. 현재 핵심 아키텍처와 전체 흐름

현재 주 흐름은 과거의 “차량기사 개인계정이 직접 신청” 방식이 아니라 **계약업체 공용계정 중심 구조**다.

```text
계약업체 공용계정 가입/로그인
  → 업체 소속 차량 등록
  → 업체 사무실 담당자가 새 출입 신청
  → 계약유형별 안전수칙 확인
  → 차량 동선 확인
  → 출입일자/차량/실제 운전자/필수서류 입력
  → 자재센터 직원 승인
  → 승인 시 신청별 기사 전용 보안링크 생성
  → 업체 담당자가 기사에게 링크 공유
  → 기사가 링크에서 안전수칙 확인
  → 현장 도착 후 사진 업로드
  → 첫 정상 현장사진 업로드 시 자동 최종완료
```

### 핵심 원칙

- 회사당 공용 업체계정 1개를 기본 단위로 사용한다.
- 사업자등록번호는 업체계정별 고유값이다.
- 로그인 ID는 업체계정, 기존 사용자계정, 직원 가입신청 범위를 포함해 중복을 막는다.
- 업체의 계약유형은 회원가입 시 결정하며 출입신청 화면에서 임의 변경할 수 없다.
- 차량기사는 신규 흐름에서 별도 회원가입이 필요 없다.
- 승인된 신청마다 별도의 기사 전용 보안링크를 발급한다.
- 기사 링크에서 안전수칙 확인 → 현장사진 업로드를 완료하면 자동 최종완료된다.
- 기존 `requests.status`는 호환성을 위해 유지하고, 실제 세부 진행단계는 `workflow_status`로 관리한다.

---

## 3. 현재 주요 파일 설명

> 파일도 사용자 영역별로 구분한다. 실제 동작 여부는 반드시 `public/index.html`의 로드 목록과 캐시 버전을 함께 확인한다.

### 3.1 공통 기반 파일

계약업체 화면과 자재센터 직원 화면 양쪽에 영향을 주거나 앱 전체 실행·레이아웃을 담당하는 파일이다.

| 파일 | 역할 |
|---|---|
| `src/worker.js` | 현재 Worker 단일 진입점. 과거 v2~v10 레이어를 내부에 동작 보존 병합하고 계약업체·직원 API를 함께 라우팅 |
| `public/index.html` | 실제 CSS/JS 로드 순서와 캐시 버전의 기준 |
| `public/js/mo-raf-guard.js` | MutationObserver 상호 재발화에 의한 UI 하드 프리즈 방지 |
| `public/css/app-width-unified-v1.css` | 모든 일반 화면 520px 폭 통일, 브라우저 85% zoom, PWA 100%, 외부 배경 통일 |
| `wrangler.toml` | Cloudflare Worker/D1 설정 |

### 3.2 계약업체·차량기사 관련 파일

계약업체 가입·정보수정·차량관리·출입신청과 승인 후 차량기사 보안링크 흐름에 관련된 파일이다.

#### 서버/API

| 파일 | 역할 |
|---|---|
| `src/company-flow-api.js` | 업체 인증, 차량관리, 업체 정보수정, 신청 관련 기본 API |
| `src/company-registration-v2.js` | 계약업체 회원가입 및 계정 생성 |
| `src/company-contract-request-v2.js` | 업체 계약유형 강제 및 신규 출입신청 wrapper |
| `src/company-driver-share-v2.js` | 승인 후 기사 링크 조회·공유 데이터 |
| `src/company-driver-access-v3.js` | 기사 보안링크 조회, 안전수칙 진행, 현장사진 업로드, 자동완료 |

#### 계약업체 화면

| 파일 | 역할 |
|---|---|
| `public/js/company-flow-v1.js` | 계약업체 기본 화면·상태·API 공통 처리 |
| `public/js/company-flow-entry-loader-v2.js` | 업체 흐름 진입 및 화면 연결 |
| `public/js/company-registration-ui-v2.js` | 업체 회원가입 UI |
| `public/js/company-home-profile-restore-v1.js` | 업체 홈 기본 구조와 정보수정 진입 보정 |
| `public/js/company-profile-ui-v1.js` | 정보수정 화면의 기본정보·차량관리·차량등록 UI 보정 |
| `public/js/company-home-vehicle-ui-v1.js` | 업체 차량 관련 화면 보정 |
| `public/js/company-home-history-tuning-v1.js` | 업체 홈, 출입이력, 날짜/상태/뒤로가기 보정 |
| `public/js/company-request-flow-v2.js` | 계약업체 신규 출입신청 전체 흐름 |
| `public/js/company-request-prefetch-v1.js` | 새 신청 진입 전에 계약정보·차량 등 데이터 사전 캐시 |
| `public/js/company-flow-ui-fix.js` | 계약업체 화면 공통 헤더·스타일·세부 UI 보정 |
| `public/js/safety-highlights-v2.js` | 계약유형별 안전수칙 화면 보정 |
| `public/js/transport-route-refined.js` | 계약유형별 차량 동선 화면 |
| `public/js/route-image-cache-v2.js` | 동선 이미지 캐시 버전 강제 갱신 |
| `public/js/application-flow-refined.js` | 업체 신청 화면 보정. 차량동선 화면은 덮어쓰지 않도록 유지 |
| `public/js/work-plan-form-editor.js` | 작업계획서 앱 내 작성·편집 기능 |
| `public/js/work-plan-stable-save-v1.js` | 작업계획서 저장 흐름 보조 |
| `public/js/work-plan-map-position-fix.js` | 작업계획서 내 동선 지도 위치 보정 |

#### 계약유형·동선 데이터

| 파일 | 역할 |
|---|---|
| `data/vehicleTypes.js` | 계약유형, 안전수칙, 동선, 필수서류 기준 데이터 |
| `public/route-images/` | 계약유형별 동선 지도 이미지 |

### 3.3 자재센터 직원·관리자 관련 파일

출입신청 승인·완료·통계와 회원관리·직원관리 등 자재센터 내부 업무에 관련된 파일이다.

#### 직원 출입신청 관리

| 파일 | 역할 |
|---|---|
| `public/js/staff-console-shell.js` | 자재센터 직원 콘솔 헤더 구성, 직원/관리자 공통 `관리자모드` 버튼 생성 |
| `public/css/staff-console-shell.css` | 직원·관리자 콘솔 헤더/레이아웃 |
| `public/js/admin-today-only.js` | 직원 화면 대기/승인/완료 탭의 오늘 기준 workflow 처리 |
| `public/js/request-list-unified-v1.js` | 직원 신청 목록을 `출입일자 | 차량번호 | 상태` 3열 구조로 통일 |
| `public/js/admin-stats-refined.js` | 자재센터 직원 통계 화면 보정 |
| `public/js/admin-excel-refined.js` | 통계 결과 Excel 내보내기 |

#### 관리자모드·계정관리

| 파일 | 역할 |
|---|---|
| `public/js/admin-tools-menu-v6.js` | 관리자모드 메뉴. 회원관리와 직원관리 권한 분기 |
| `public/js/staff-account-management.js` | 직원 계정 관련 관리 기능 |
| `public/js/staff-member-management.js` | 직원/회원 관리 화면 기능 |
| `public/js/driver-account-management-v2.js` | 레거시 차량기사 회원 관리 |
| `public/js/password-reset-flow-v2.js` | 비밀번호 초기화 관련 직원 관리 흐름 |

---

# 4. 계약업체 영역

아래 4번 전체는 **계약업체 사무실 담당자 및 해당 업체 차량기사**가 사용하는 기능이다. 자재센터 직원 화면과 혼동하지 않는다.

## 4.1 계약업체 계정과 회원가입

관련 서버:

- `src/company-registration-v2.js`
- `src/company-flow-api.js`

현재 회원가입 입력 순서:

1. 아이디
2. 비밀번호
3. 비밀번호 확인
4. 업체명
5. 사업자번호
6. 업체 연락처
7. 계약유형
8. 회원가입

주요 규칙:

- 담당자명 입력은 신규 UI에서 제거되어 있다.
- `contact_name` DB 필드는 호환성을 위해 남아 있으나 신규 가입에서는 비사용한다.
- 사업자등록번호는 숫자 10자리 기준으로 중복 검사한다.
- 로그인 ID는 영문 대소문자를 구분하지 않고 중복 검사한다.
- 중복 범위는 `company_accounts`, 기존 `users`, 승인/대기 중인 `staff_applications`까지 포함한다.
- 업체가입 후 별도 관리자 승인 없이 `active` 상태로 즉시 사용할 수 있다.
- 비밀번호는 PBKDF2-SHA256 100,000회 반복을 사용한다.

### 계약유형 표시명

현재 UI 표시는 다음과 같이 “차량” 문구를 제거해 통일했다.

- 공사업체
- 물자수송용역
- 기자재 납품
- 불용품 매각
- PCBs처리용역

`data/vehicleTypes.js` 내부 원본 명칭 일부에는 기존 “차량” 표현이 남아 있을 수 있으므로 **UI 표시명과 내부 ID를 구분**한다.

---

## 4.2 업체 정보수정과 차량관리

2026-08-09 기준 업체 홈의 기존 `회원정보 수정`은 **`정보수정`**으로 정리되었고, 정보수정 화면 안에서 기본정보와 차량관리를 함께 처리한다.

### 기본정보

수정 가능한 주요 항목:

- 업체명(상호)
- 사업자번호
- 업체 연락처

사업자번호 변경 시:

- 숫자 10자리 검증
- 다른 업체와의 중복 검사를 서버에서 수행

### 차량관리

업체는 여러 대의 소속 차량을 등록할 수 있다.

차량 마스터 주요 값:

- 차량번호
- 기본 운전자명
- 기본 운전자 연락처

현재 정보수정 화면은 다음 3개 영역으로 구분한다.

```text
기본정보
차량관리      ← 등록된 차량 목록
차량 등록     ← 신규 차량 입력/저장
```

- `차량관리` 영역은 등록 차량 목록 중심이다.
- 신규 차량 입력은 별도 `차량 등록` 영역으로 분리했다.
- 차량 등록/수정/삭제 기능은 유지한다.
- `default_vehicle_type_id`는 호환성 때문에 DB에 남아 있으나 차량관리 UI에서 차량유형을 따로 받지 않는다.
- 신청 차량유형은 업체계정의 계약유형으로 강제한다.

---

## 4.3 계약업체 홈 화면

현재 홈 제목은 `출입 신청 관리`다.

주요 진입 기능:

- 헤더의 `정보수정`
- `로그아웃`
- 본문의 `새 출입 신청`
- `출입 이력`

차량관리는 별도 홈 버튼보다 **정보수정 화면 안으로 통합**하는 방향이다.

### 진행 중 신청 카드

한 줄에 다음 세 항목만 표시한다.

```text
출입일자 | 차량번호 | 진행상태
```

- 출입일자에서 연도는 숨긴다.
- 날짜와 차량번호의 글자 크기/굵기는 동일하게 맞춘다.
- 홈 표시 상태명:
  - `승인대기`
  - `안전수칙`
  - `현장사진`
  - `최종완료`
  - `반려`

### 출입 이력

- `workflow_status = completed`인 최종완료 건만 표시한다.
- 출입일자 기준 내림차순으로 정렬한다.
- 별도 X 버튼 없이 Android/브라우저 뒤로가기로 업체 홈에 복귀한다.

---

## 4.4 새 출입 신청 흐름

관련 프런트:

- `public/js/company-request-flow-v2.js`
- `public/js/company-request-prefetch-v1.js`
- `public/js/safety-highlights-v2.js`
- `public/js/transport-route-refined.js`
- `public/js/application-flow-refined.js`

현재 흐름:

```text
새 출입 신청
  → 필수 안전수칙
  → 기타 안전수칙
  → 차량 동선
  → 출입신청 정보/서류 입력
  → 신청 제출
```

### 계약유형 강제

업체가 회원가입할 때 선택한 `contract_type_id`가 출입신청의 유형이 된다.

서버는 `/api/company/requests` 제출 시 화면에서 전달된 유형을 그대로 신뢰하지 않고 **로그인된 업체계정의 계약유형으로 다시 덮어쓴다.**

### 출입일자 기본값

- 기본값은 다음 영업일이다.
- 토요일·일요일과 앱에 정의된 대한민국 공휴일/대체공휴일을 제외한다.
- 하드코딩된 공휴일 목록은 2026~2027년 기준이므로 이후 연도는 갱신이 필요하다.

### 차량 선택

- 등록차량 선택 시 차량번호를 읽기전용으로 자동 표시한다.
- 기본 운전자명·연락처를 자동 채운다.
- 실제 당일 운전자가 다르면 신청 화면에서 운전자명·연락처만 변경할 수 있다.
- 신청별 운전자 변경은 차량 마스터 기본정보를 수정하지 않는다.
- 목록 마지막의 `용차`를 선택하면 차량번호·운전자명·연락처를 직접 입력한다.

### 화면 전환 성능

`company-request-prefetch-v1.js`가 업체 홈에서 필요한 데이터를 미리 가져와 캐시한다.

차량 등록·수정·삭제 시 차량 캐시는 폐기한다.

---

## 4.5 안전수칙과 차량 동선

기준 데이터: `data/vehicleTypes.js`

| 내부 ID | UI 계약유형 | 신청번호 접두어 |
|---|---|---:|
| `construction` | 공사업체 | A |
| `transport` | 물자수송용역 | B |
| `delivery` | 기자재 납품 | C |
| `scrap` | 불용품 매각 | D |
| `pcbs` | PCBs처리용역 | E |

### 공통 필수 안전수칙 6개

1. 안전장구(안전모, 안전화) 착용
2. 운전위치 이탈 시 시동 정지, 파킹브레이크 체결, 고임목 설치
3. 작업차량 작업반경 내 출입 금지
4. 음주 후 현장 투입 및 작업 금지
5. 적재물 덮개 작업 시 안전대 착용 및 안전고리 체결
6. 혹한·혹서기 충분한 휴식 시행

### 공통 기타 안전수칙

- 자재센터 내 제한속도 20km 준수
- 교차지점 일단정지
- 안전띠 착용
- 승차석 외 탑승 금지
- 운전면허 및 관련 자격 소지

### 동선 지도

지도 자산:

```text
public/route-images/
├── construction.jpg
├── transport.jpg
├── scrap.jpg
└── pcbs.jpg
```

주요 목적지:

- 물자수송/납품: 전선 야적장
- 공사업체: 3창고
- 불용품 매각: 고철장
- PCBs: 폐변압기 야적장

`transport-route-refined.js`가 계약유형별 지도를 선택하며, 2026-08-09에는 모든 계약유형에서 해당 동선 이미지가 올바르게 선택되도록 보정했다.

`application-flow-refined.js`가 route 화면을 다시 덮어쓰지 않도록 유지해야 한다.

---

## 4.6 필수 제출서류

공사업체·불용품 매각·PCBs 처리 유형은 작업 관련 서류가 필요하다.

| 키 | 표시명 | 비고 |
|---|---|---|
| `workPlan` | 작업계획서 | 앱 내 작성·저장 기능 사용 |
| `tbm` | TBM | 앱 내 필기/첨부 기능 사용 |
| `safetyChecklist` | 위험성 체크리스트 | 앱 내 필기/첨부 기능 사용 |
| `sitePhoto` | 현장사진 | 업체 사전신청 단계에서는 받지 않고 기사 전용 링크에서 현장 도착 후 업로드 |

작업계획서 관련 과거 저장 실패·시도·디버깅 기록은 **해결 완료된 이력**이므로 이 컨텍스트 문서에서는 더 이상 관리하지 않는다.

파일 제한은 서버에서 개별 파일 약 5MB 기준으로 검증하며 현재 문서는 D1 `documents` 테이블에 base64 형태로 저장한다.

---

## 4.7 승인 후 차량기사 전용 링크

관련 서버:

- `src/company-driver-share-v2.js`
- `src/company-driver-access-v3.js`
- `src/company-flow-api.js`

자재센터 직원이 업체 신청을 승인하면 신청별 기사 접근 토큰이 생성된다.

업체 담당자는 승인된 신청 상세에서 링크를 확인하고 기사에게 전달한다.

공유 방식:

- 모바일 네이티브 공유
- 카카오톡 등 사용자가 직접 선택하는 공유
- 링크 복사

기사 링크는 신청별 토큰을 사용하며 **출입예정일 다음 날 23:59:59 KST까지** 유효하도록 설계되어 있다.

기사 진행상태:

```text
safety_pending
  → 기사 안전수칙 확인
photo_pending
  → 현장사진 업로드
completed
```

첫 번째 정상 현장사진 업로드 성공 시 서버가 즉시 `workflow_status='completed'`, `photo_uploaded_at`, `completed_at`을 기록한다.

---

## 4.8 계약업체 관점 신청 상태

### 레거시 상태

`requests.status`

- `pending`
- `approved`
- `rejected`

### 실제 세부 진행상태

`requests.workflow_status`

```text
pending
  → 자재센터 직원 승인
safety_pending
  → 기사 안전수칙 확인
photo_pending
  → 기사 현장사진 업로드
completed
```

업체 화면은 `workflow_status`를 우선해 다음처럼 표시한다.

- 승인대기
- 안전수칙
- 현장사진
- 최종완료
- 반려

---

# 5. 자재센터 직원·관리자 영역

아래 5번 전체는 **자재센터 직원과 관리자 계정**이 사용하는 기능이다. 계약업체 화면과 별도 흐름이다.

## 5.1 직원과 관리자 권한 구분

### 일반 자재센터 직원

주요 기능:

- 출입신청 조회
- 신청 상세 및 첨부서류 확인
- 승인/반려
- 승인된 신청 진행상태 확인
- 통계/검색
- Excel 내보내기
- 차량기사 회원관리
- 관리자모드 진입

### 관리자

일반 직원 기능을 모두 포함하며 추가로 다음 기능을 사용한다.

- 직원 가입신청 승인·반려
- 직원 사용중지·재개
- 직원 권한 변경
- 직원 계정 삭제
- 관리자 권한 부여/회수

### 관리자모드 버튼 원칙

`staff-console-shell.js`는 **직원과 관리자 모두에게 헤더의 `관리자모드` 버튼을 표시**한다.

`admin-tools-menu-v6.js`에서 권한을 나눈다.

- `회원관리`: 일반 직원도 사용 가능
- `직원관리`: 관리자만 사용 가능
- 일반 직원이 직원관리를 누르면 권한 제한 안내를 표시

즉 버튼 자체를 역할별로 숨기지 않고, **기능 진입 시 권한을 판정**하는 구조다.

---

## 5.2 출입 신청 관리 화면

상단 주요 탭:

```text
대기 | 승인 | 완료 | 통계
```

### 대기

- 직원 승인을 기다리는 `status=pending` 신청

### 승인

출입일자가 **오늘**이며 신규 업체 흐름에서 다음 상태인 신청만 표시한다.

- `safety_pending`
- `photo_pending`

기사가 현장사진까지 완료해 `completed`가 되면 승인 숫자와 목록에서 빠진다.

### 완료

출입일자가 **오늘**이며 다음 조건을 만족하는 신청:

- `companyFlow=true`
- `workflowStatus=completed`

탭 버튼은 `완료`, 신청 카드의 상태 배지는 `최종완료`로 표시한다.

### 통계

기간·방문목적·차량번호·계약업체 등으로 조회한다.

---

## 5.3 직원 목록·상세·자동 갱신

직원용 `GET /api/requests` 응답은 신규 업체 신청에 다음 값을 포함한다.

- `workflowStatus`
- `companyFlow: true`

따라서 직원 화면은 레거시 `status`만 보지 않고 실제 기사 진행상태를 표시한다.

신청 목록은 모바일 폭을 고려해 다음 3열 구조를 사용한다.

```text
출입일자 | 차량번호 | 상태
```

### 5초 폴링 원칙

직원 화면은 최신 상태 반영을 위해 주기적으로 데이터를 다시 확인한다.

2026-08-08 이후에는 **데이터가 실제로 바뀐 경우에만 재렌더**하도록 수정해 5초마다 화면이 깜빡이는 현상을 줄였다.

완료 탭을 선택한 상태도 불필요한 재렌더 때문에 승인 탭으로 되돌아가지 않도록 유지한다.

---

## 5.4 계약업체 관리

직원/관리자 측 업체관리 화면에서는 계약업체 계정을 조회·관리한다.

현재 주요 기능:

- 업체 목록 조회
- 업체명과 사업자번호 표시
- 계약유형과 연락처 표시
- 업체 상세 진입
- 업체 계정 삭제
- 업체 비밀번호 임시 초기화

업체 삭제 시 관련 서버 권한·CSRF 검증을 거치며 소속 차량·세션 등 연결 데이터도 함께 정리하는 구조다.

업체 차량을 관리자 화면에서 별도로 직접 관리·삭제하는 기능은 불필요하다고 판단해 롤백했으며, 차량 관리는 기본적으로 **계약업체의 정보수정 화면**에서 처리한다.

---

## 5.5 직원관리와 레거시 차량기사 회원관리

### 직원관리

관리자 전용 기능:

- 직원 가입신청 승인/반려
- 직원 사용중지/재개
- 권한 변경
- 직원 계정 삭제
- 관리자 권한 부여/회수

### 차량기사 회원관리

신규 계약업체 흐름에서는 기사 계정이 필요 없지만, 기존 데이터와 운영 호환을 위해 레거시 회원관리 기능을 유지한다.

대표 기능:

- 차량번호 기반 기사 계정 조회
- 임시 비밀번호 발급
- 비밀번호 발급 요청 처리
- 차주 변경
- 논리적 회원 삭제
- 기존 출입기록 보존

관련 파일:

- `src/driver-account-staff-api.js`
- `src/password-reset-staff-api.js`
- `public/js/driver-account-management-v2.js`
- `public/js/password-reset-flow-v2.js`

---

## 5.6 통계와 Excel

통계 화면은 다음 기준을 조합해 검색한다.

- 기간
- 방문목적
- 차량번호
- 계약업체

Excel 내보내기는 `public/js/admin-excel-refined.js`에서 브라우저 내부적으로 XLSX 구조를 생성한다.

향후 공식화 시 Excel의 상태명도 레거시 `status`보다 `workflowStatus`를 우선 반영하도록 일관성을 확인한다.

---

## 6. 주요 데이터 구조

### `company_accounts`

- `id`
- `login_id`
- `login_id_norm`
- `company_name`
- `business_no`
- `business_no_norm`
- `contact_name` — 호환 필드, 신규 UI에서는 비사용
- `phone`
- `contract_type_id`
- `salt`
- `hash`
- `account_status`
- `created_at`

### `company_sessions`

업체 Bearer 세션 토큰을 저장한다.

업체 세션 최대기간은 현재 약 30일이다.

### `company_vehicles`

- `company_account_id`
- `vehicle_number`
- `driver_name`
- `driver_phone`
- `default_vehicle_type_id` — 호환 필드

업체 내 차량번호는 unique다.

### `requests`

레거시 필드에 신규 업체 흐름 필드가 추가되어 있다.

주요 신규 필드:

- `workflow_status`
- `company_account_id`
- `company_vehicle_id`
- `is_temporary_vehicle`
- `driver_access_token`
- `driver_access_expires_at`
- `safety_confirmed_at`
- `photo_uploaded_at`
- `completed_at`

### `documents`

신청별 첨부문서를 base64로 저장한다.

---

## 7. 현재 Worker 구조

현재 진입점은 `src/worker.js` 단일 파일이다.

과거에는 `worker.js + worker-v2..v10`으로 이어지는 레이어 오버라이드 체인이었으나 2026-08-08에 새 담당자의 혼동을 줄이기 위해 **동작을 보존하며 하나의 파일로 병합**했다.

각 과거 버전은 블록 스코프로 격리된 레이어로 남아 있다.

```text
요청 → LAYER_v10 → v9 → v8 → v7 → v6 → v5 → v4 → v3 → v2 → LAYER_base
export default LAYER_v10
```

병합은 로직 재작성이 아니라 기존 파일 본문을 내부 레이어로 옮긴 방식이므로 라우트 우선순위·미들웨어·cron 동작을 보존한다.

`LAYER_v10`은 신규 계약업체 흐름을 우선 처리하며 직원용 API에도 신규 workflow 상태를 합친다.

---

## 8. 보안 관련 현재 상태

### 적용 중

- PBKDF2-SHA256 100,000회
- 랜덤 salt
- 직원 세션 쿠키 기반 인증
- 업체 Bearer 세션
- 직원 cookie write 요청의 CSRF 보호
- 주요 응답 no-store
- 보안헤더 적용
- 기사 현장사진 JPG/PNG magic byte 검사
- 사진 개별 5MB 제한
- 기사 링크 만료시간 검사

### 기술 부채/추가 검토

- 업체 Bearer 토큰이 프런트 localStorage에 남는 구조
- 기사 접근토큰이 raw token 형태로 D1에 저장되는 구조
- D1 첨부문서 base64 저장의 확장성
- 정식 migration 체계 부재
- 프런트 보정 스크립트 누적
- 정식 보안진단 미실시

`src/worker.js`의 최외곽 레이어는 기사 사진 업로드를 위해 `Permissions-Policy`의 camera를 same-origin에 허용한다.

---

## 9. PWA·캐시·화면 안정화 원칙

과거 실기기에서 이전 JavaScript가 계속 보이거나 화면이 순간적으로 이전 형태로 보이는 문제가 반복되었다.

현재 원칙:

- `public/index.html`에서 주요 JS/CSS에 쿼리버전을 사용한다.
- Worker 응답에서 JS/CSS/HTML을 no-store 처리한다.
- Service Worker도 최신 코드 반영을 우선한다.
- 실제 동작 파일인지 판단할 때 `public/index.html` 로드 여부를 먼저 확인한다.

### 공통 화면 규격

`public/css/app-width-unified-v1.css` 기준:

- 일반 앱 화면의 논리 폭은 최대 520px
- 일반 브라우저 탭에서는 전체 UI에 85% zoom 적용
- 설치형 PWA/standalone은 100% 크기 유지
- 브라우저 하단까지 좌·중앙·우 배경이 끊기지 않도록 3구역 배경 유지
- #app 밖에 생성되는 관리자/이력/관리 레이어도 동일 폭에 정렬

### 공통 헤더

- 주요 화면 헤더 높이 68px로 통일
- 제목 아래 부가설명은 숨김

### MutationObserver 프리즈 방지

여러 보정 스크립트가 서로 DOM 변경을 재발화해 관리자 화면이 하드 프리즈되던 문제가 있었다.

`public/js/mo-raf-guard.js`가 프레임 단위 실행 예산을 적용해 상호 재발화 폭주를 막는다.

이 파일은 **초기 로드 순서가 중요**하므로 제거하거나 뒤로 이동하지 않는다.

---

## 10. 레거시 차량기사 계정과 신규 기사 링크 구분

신규 계약업체 흐름:

```text
업체 공용계정 신청
→ 직원 승인
→ 기사 무계정 보안링크
→ 안전수칙
→ 현장사진
→ 완료
```

레거시 흐름:

```text
차량기사 회원계정
→ 기존 기사 로그인/회원관리 기능
```

두 흐름은 동시에 저장소에 존재한다.

**신규 계약업체 신청에서 차량기사 회원가입을 요구하면 안 된다.**

---

## 11. 현재 검증 수준

확인한 범위:

- Galaxy/Android 실제 화면을 통한 반복 UI 확인
- 계약업체 가입/로그인/홈/정보수정/차량관리 흐름
- 계약업체 신규 신청 화면과 차량동선
- 직원 대기/승인/완료/통계 탭
- 직원/관리자 공통 관리자모드 버튼과 권한 분기
- 기사 안전수칙/현장사진/자동완료 흐름 구현
- MutationObserver 프리즈 및 주기적 재렌더 깜빡임 개선
- 브라우저/PWA 화면 폭과 배경 통일

아직 부족한 부분:

- 전체 신규 업체 흐름 자동 E2E 테스트 없음
- GitHub Actions CI 없음
- 정식 운영 수준의 부하/장애/보안 테스트 없음

GitHub commit status의 `statuses: []`는 “CI 통과”가 아니라 **CI 상태검사가 구성되어 있지 않음**을 의미한다.

---

## 12. 다음 개발자/AI가 반드시 알아야 할 것

1. 현재 주 아키텍처는 **계약업체 공용계정 + 기사 무계정 보안링크**다.
2. 문서를 볼 때 **4번 계약업체 영역**과 **5번 자재센터 직원·관리자 영역**을 분리해서 판단한다.
3. 과거 차량기사 회원계정 흐름은 레거시이며 신규 업체 흐름과 혼동하지 않는다.
4. 업체의 출입 유형은 신청마다 고르는 것이 아니라 가입 시 확정된 `contract_type_id`로 강제한다.
5. 자재센터 직원 화면은 레거시 `status`만 보면 안 되고 반드시 `workflowStatus`를 함께 본다.
6. `완료` 탭은 오늘의 `workflowStatus=completed` 신규 업체 신청을 의미한다.
7. 업체 `출입 이력`은 최종완료 신청만 포함한다.
8. `관리자모드` 버튼은 직원/관리자 공통 노출이며, 권한 없는 기능은 진입 시 제한한다.
9. 동선 화면 body를 `application-flow-refined.js`에서 다시 교체하면 과거 placeholder 회귀가 생길 수 있다.
10. 관리자/완료 화면 프리즈 방지를 위해 `mo-raf-guard.js` 로드 순서를 유지한다.
11. 수정 전 최신 파일과 SHA를 다시 확인하고 실제 `public/index.html` 로드 여부를 확인한다.

---

## 13. 유지관리 원칙

- 계약유형/안전수칙/필수서류 변경 전 `data/vehicleTypes.js` 확인
- 권한 변경 시 프런트 UI와 서버 권한 검사를 함께 수정
- 기존 신청기록을 삭제하는 변경 금지
- DB 스키마 변경 시 정식 migration 도입 권장
- 지도·양식 이미지는 앱 내부 정적 자산 사용
- PWA/모바일 회귀는 실제 Android에서 확인
- `public/index.html`에 로드되지 않는 보정 파일은 실제 동작 코드로 간주하지 않음
- DOM 보정 코드는 가능하면 멱등적으로 작성해 MutationObserver 상호루프를 만들지 않음
- 운영 비밀값, 토큰, 비밀번호, D1 식별자 등은 본 문서에 기재하지 않음
- 기능 변경 시 이 문서도 함께 갱신

# Entry-pass UI 디자인 가이드

## 1. 목적

이 문서는 Entry-pass 앱 전체 화면을 일관성 있게 개선하기 위한 구현 기준이다.

핵심 목표는 다음과 같다.

- 현장 사용자가 짧은 시간 안에 내용을 이해할 수 있는 높은 가독성
- 화면마다 같은 앱으로 인식되는 일관된 시각 체계
- 스마트폰에서 한 손으로 조작하기 쉬운 충분한 터치 영역
- 안드로이드·iOS의 상태 표시줄 및 하단 Safe Area 대응
- 공통 스타일 변경이 다른 기능을 훼손하지 않도록 화면별 적용 범위를 명확히 관리

현재 완성한 `필수 안전수칙` 화면을 전체 디자인의 기준 화면으로 사용한다.

---

## 2. 디자인 원칙

### 2.1 가독성 우선

- 장식보다 정보 전달을 우선한다.
- 한국어 본문은 단어 중간에서 줄바꿈하지 않는다.
- 긴 문장은 적절히 줄바꿈하되, 핵심 행동이나 위험 요소는 색상과 굵기로 강조한다.
- 자간은 기본적으로 `0`을 사용한다.
- 본문 줄간격은 `1.45~1.55` 범위에서 화면 목적에 맞게 적용한다.

### 2.2 일관성은 유지하되 화면 목적에 따라 변형

모든 화면을 똑같이 만들지 않는다. 대신 다음 요소는 공통 체계를 유지한다.

- 색상
- 글꼴과 글자 크기 계층
- 카드와 버튼의 모서리 반경
- 화면 좌우 여백
- 입력창 높이
- 상태·경고·완료 표현 방식
- 차량 유형 아이콘 사용 방식

### 2.3 모바일 우선

- 기준 화면 폭은 스마트폰 세로 화면이다.
- 최대 콘텐츠 폭은 현재 앱 기준인 `520px`을 유지한다.
- 높이 계산에는 필요 시 `100dvh`를 사용한다.
- 하단 버튼과 입력 영역은 `env(safe-area-inset-bottom)`을 반영한다.
- 터치 가능한 요소는 최소 `48px` 높이를 확보한다.

### 2.4 화면 전체 높이 활용

- 콘텐츠가 적은 화면은 하단에 어색한 빈 공간이 남지 않도록 flex 레이아웃을 사용한다.
- 주요 CTA는 필요한 화면에서 하단에 자연스럽게 배치한다.
- 작은 화면에서 스크롤이 생기는 경우 콘텐츠를 무리하게 축소하지 않고 정상 스크롤을 허용한다.
- 화면을 꽉 채우기 위해 글자 크기나 줄간격을 지나치게 줄이지 않는다.

### 2.5 단계적 적용

- 공통 CSS를 한 번에 대규모 변경하지 않는다.
- 화면군별로 수정하고 실제 스마트폰에서 확인한 후 다음 화면군으로 진행한다.
- 임시 후처리 스크립트는 검증 단계에서 사용할 수 있으나, 전체 개편 단계에서는 가능한 한 `app.js`의 정식 렌더링 구조와 공통 CSS로 통합한다.

---

## 3. 디자인 토큰

전체 개편 시 `public/css/styles.css`의 `:root` 변수를 아래 기준으로 정리한다.

```css
:root {
  --color-bg: #F8FAFC;
  --color-surface: #FFFFFF;
  --color-surface-subtle: #F8FAFC;
  --color-header: #0F172A;

  --color-text: #0F172A;
  --color-text-muted: #64748B;
  --color-border: #E2E8F0;

  --color-primary: #2563EB;
  --color-primary-pressed: #1D4ED8;
  --color-primary-disabled: #94A3B8;

  --color-danger: #DC2626;
  --color-danger-bg: #FEF2F2;
  --color-warning: #D97706;
  --color-warning-bg: #FEF3C7;
  --color-success: #16A34A;
  --color-success-bg: #DCFCE7;

  --radius-sm: 10px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 18px;

  --shadow-card: 0 10px 25px rgba(0, 0, 0, 0.06);

  --space-screen-x: 16px;
  --space-section: 16px;
  --control-height: 56px;
}
```

기존 변수명은 화면별 변경 과정에서 점진적으로 위 체계에 맞춘다. 한 번에 모든 이름을 바꾸지 않는다.

---

## 4. 타이포그래피

### 4.1 기본 글꼴

```css
font-family: Pretendard, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

Pretendard를 앱 전체 기본 글꼴로 사용한다. 외부 CDN을 사용할 경우 네트워크 실패 시 시스템 글꼴로 자연스럽게 대체되어야 한다.

### 4.2 글자 계층

| 용도 | 크기 | 굵기 | 줄간격 | 자간 |
|---|---:|---:|---:|---:|
| 주요 화면 제목 | 26~28px | 800 | 1.2 | 0 |
| 카드·섹션 핵심 제목 | 20~22px | 700~800 | 1.3 | 0 |
| 중요 본문·안전수칙 | 19px | 600 | 1.5~1.55 | 0 |
| 일반 본문·입력값 | 17~18px | 500~600 | 1.45~1.5 | 0 |
| 보조 설명 | 15~16px | 500 | 1.4~1.5 | 0 |
| 핵심 단어 강조 | 본문과 동일 | 700 | 상속 | 0 |

### 4.3 강조 규칙

- 핵심 행동이나 장비명은 `--color-primary`와 `font-weight: 700`을 사용한다.
- 위험·금지·오류는 `--color-danger`를 사용한다.
- 한 문장 전체를 강조색으로 칠하지 않고 핵심 단어만 강조한다.
- 색상만으로 상태를 구분하지 않고 문구, 아이콘, 배지 중 하나를 함께 사용한다.

---

## 5. 공통 레이아웃

### 5.1 화면 배경과 좌우 여백

- 기본 배경: `#F8FAFC`
- 기본 좌우 여백: `16px`
- 정보 밀도가 높은 화면은 최소 `14px`까지 허용한다.
- 콘텐츠 카드 간 기본 간격: `12~16px`
- 큰 섹션 간 간격: `20~24px`

### 5.2 헤더

기본 헤더는 다음 두 유형으로 나눈다.

#### A. 신청 단계형 헤더

- 차량 유형 아이콘 + 현재 화면 제목을 한 줄에 표시
- 차량명은 반복 표시하지 않는다.
- 스마트폰 시스템 뒤로가기로 충분한 단계에서는 앱 내부 뒤로가기 버튼을 생략할 수 있다.
- 단, 웹 브라우저 환경이나 진입 경로상 이전 단계 복귀가 반드시 필요한 화면은 별도 검토한다.
- 높이: 약 `64~72px`
- 배경: `#0F172A`
- 제목: `26~28px / 800 / 자간 0`
- 아이콘: `32~34px`

예시:

```text
🚛  필수 안전수칙
🚛  기타 안전수칙
🚛  차량 동선 안내
🚛  신청 정보 및 서류
```

#### B. 계정·관리형 헤더

- 로그인, 회원가입, 기사 홈, 직원 화면 등에 사용한다.
- 화면 제목과 필요한 보조 정보 또는 로그아웃 버튼을 표시한다.
- 이전 화면으로 돌아갈 경로가 명확히 필요한 경우에만 앱 내부 뒤로가기를 사용한다.

### 5.3 진행 표시줄

- 신청 단계 화면에서만 사용한다.
- 완료·현재 단계: `--color-primary`
- 미진행 단계: `--color-border`
- 높이: `4~5px`
- 헤더와 본문 사이에서 일정한 위치와 여백을 유지한다.

---

## 6. 공통 컴포넌트

### 6.1 카드

```css
background: #FFFFFF;
border: 0;
border-radius: 16px;
box-shadow: 0 10px 25px rgba(0,0,0,.06);
```

- 카드 내부 패딩: `14~18px`
- 리스트형 카드는 항목 사이에 `#E2E8F0` 구분선을 사용한다.
- 카드 중첩은 피한다.
- 단순 정보 행에는 과도한 그림자를 사용하지 않는다.

### 6.2 버튼

#### 기본 CTA

- 높이: `56px`
- 배경: `#2563EB`
- 글자: 흰색, `18~19px / 700`
- 모서리: `16px`
- 자간: `0`
- 비활성: `#94A3B8`
- 눌림: `#1D4ED8`

#### 보조 버튼

- 흰 배경 + `#E2E8F0` 테두리
- 기본 글자색 `#0F172A`
- 주요 CTA와 시각적 우선순위가 충돌하지 않도록 한다.

### 6.3 입력창과 선택창

- 최소 높이: `52~56px`
- 글자 크기: `17~18px`
- 배경: 흰색 또는 매우 옅은 회색
- 테두리: `#E2E8F0`
- 모서리: `12px`
- 포커스: `#2563EB` 테두리와 약한 포커스 링
- 라벨과 입력값의 대비를 명확히 한다.
- 모바일 키보드가 올라왔을 때 버튼이나 현재 입력칸이 가려지지 않도록 한다.

### 6.4 배지와 상태

- 필수: 빨간색 계열
- 선택: 회색 계열
- 승인: 초록색 계열
- 대기: 주황색 계열
- 반려: 빨간색 계열
- 배지 문구는 1~3단어로 간결하게 유지한다.

### 6.5 리스트

- 목록 본문은 최소 `17px` 이상을 사용한다.
- 번호, 아이콘, 본문 시작 위치를 화면 내에서 통일한다.
- 2줄 이상인 항목은 번호를 첫 줄 기준 상단 정렬한다.
- 항목별 상하 패딩은 정보 밀도에 따라 `12~16px`을 사용한다.

---

## 7. 차량 유형 아이콘

현재 차량 유형 데이터의 아이콘을 화면 전반에서 재사용한다.

| 차량 유형 | 아이콘 |
|---|---|
| 물자수송용역 차량 | 🚛 |
| 공사업체 차량 | 🏗️ |
| 기자재 납품차량 | 🚚 |
| 불용품 매각차량 | ♻️ |
| PCBs처리용역 차량 | ☣️ |

- 아이콘과 화면 제목 사이 간격은 `10~12px`을 사용한다.
- 아이콘 자체가 버튼처럼 보이지 않도록 배경 박스를 과도하게 사용하지 않는다.
- 추후 SVG 아이콘으로 교체하더라도 크기와 정렬 기준은 동일하게 유지한다.

---

## 8. 화면별 적용 방향

### 8.1 1차: 기사 신청 핵심 플로우

적용 순서:

1. 차량 유형 선택
2. 필수 안전수칙 — 기준 화면, 현재 1차 완료
3. 기타 안전수칙
4. 차량 동선 안내
5. 신청 정보 및 제출서류
6. 신청 결과

핵심 기준:

- 차량 아이콘 + 현재 단계 제목 헤더
- 동일한 진행 표시줄
- 일관된 카드, 버튼, 화면 여백
- 중요 정보만 선택적으로 파란색 강조
- 주요 CTA의 위치와 높이 통일

### 8.2 2차: 로그인·회원가입·기사 홈

- 입력 폼의 라벨과 입력창 정렬 개선
- 회원가입 항목이 많은 화면의 스크롤과 키보드 대응
- 기사 홈에서 신규 신청 CTA와 신청 이력의 정보 우선순위 정리

### 8.3 3차: 기사 기본정보·신청 이력

- 정보 행, 수정 폼, 상태 배지 통일
- 빈 상태와 오류 상태 디자인 정리

### 8.4 4차: 직원 승인·관리 화면

- 모바일에서 표 형태를 그대로 사용하지 않는다.
- 신청 건을 카드 또는 요약 행으로 표시한다.
- 승인·반려 액션은 명확히 분리한다.
- 위험한 작업은 확인 절차를 둔다.

---

## 9. 접근성 및 사용성

- 본문 대비는 WCAG AA 수준을 목표로 한다.
- 터치 영역은 최소 `48 × 48px`을 확보한다.
- 아이콘만 있는 버튼에는 `aria-label`을 지정한다.
- 비활성 버튼은 색상뿐 아니라 실제 `disabled` 속성을 사용한다.
- 포커스 표시를 제거하지 않는다.
- 오류 메시지는 입력창 근처에 구체적으로 표시한다.
- 안드로이드 시스템 뒤로가기와 브라우저 히스토리가 정상 동작해야 한다.

---

## 10. 구현 유의사항

1. 화면 전체를 `:has()` 선택자와 후처리 스크립트에만 의존하지 않는다.
2. 검증이 끝난 디자인은 `app.js` 렌더링 마크업과 공통 CSS에 정식 통합한다.
3. 공통 클래스 변경 전 해당 클래스가 사용되는 모든 화면을 확인한다.
4. 화면별 예외는 전용 클래스 또는 화면 루트 클래스로 범위를 제한한다.
5. 인라인 스타일은 점진적으로 공통 클래스로 이동한다.
6. `100vh`보다 모바일 주소창 변화에 안정적인 `100dvh`를 우선 검토한다.
7. Safe Area를 중복 적용해 하단 여백이 과도해지지 않도록 한다.
8. 실제 기기 확인 전 자동으로 완성됐다고 판단하지 않는다.
9. 이미지 시안 생성으로 코드 수정 작업을 대체하지 않는다.
10. 기존 기능, 데이터 저장, 화면 이동 로직은 UI 변경과 분리한다.

---

## 11. 화면별 검수 체크리스트

각 화면 수정 후 다음 항목을 확인한다.

- [ ] Pretendard가 적용되었는가
- [ ] 제목·본문·보조 문구의 크기 계층이 명확한가
- [ ] 자간이 `0`으로 통일되었는가
- [ ] 화면 좌우 여백이 균형적인가
- [ ] 카드 사이 간격이 일정한가
- [ ] 버튼 높이와 모서리 반경이 기준에 맞는가
- [ ] 핵심 CTA가 화면에서 명확한가
- [ ] 긴 한국어 문장이 단어 중간에서 잘리지 않는가
- [ ] 작은 화면에서 필요한 스크롤이 정상 동작하는가
- [ ] 큰 화면에서 하단에 어색한 빈 공간이 남지 않는가
- [ ] 상태 표시줄과 하단 시스템 내비게이션 영역을 침범하지 않는가
- [ ] 시스템 뒤로가기가 정상 동작하는가
- [ ] 다른 화면의 UI나 기능에 부작용이 없는가
- [ ] 실제 스마트폰에서 확인했는가

---

## 12. 다음 작업

다음 단계는 `차량 유형 선택` 화면부터 시작한다.

1. 현재 마크업과 사용성 점검
2. 기준 디자인에 맞는 1차 수정
3. 실제 스마트폰 화면 확인
4. 피드백 반영
5. 승인 후 기타 안전수칙 및 이후 신청 단계로 확장

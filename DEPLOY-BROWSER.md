# 터미널 없이 브라우저로만 배포하기

PC의 명령어 창(터미널) 없이, **Cloudflare 대시보드 + GitHub 웹**만으로 배포하는
방법입니다. 인터넷 브라우저만 있으면 됩니다.

> 서류(작업계획서 사진 등)는 **D1에 저장**하므로 **R2·결제카드가 필요 없습니다.**
> 업로드 시 이미지를 자동으로 압축(긴 변 1600px·JPEG)하여 용량을 크게 줄입니다.

핵심은 **최초 1회 배포**입니다. 한 번 해두면 진짜 주소(`...workers.dev`)가 생기고,
이후 제가 코드를 수정해 GitHub에 올리면 **자동으로 다시 배포**됩니다.

전체 순서: ① Cloudflare 가입 → ② D1 만들기 → ③ ID 저장(자동) →
④ D1에 표 만들기(스키마) → ⑤ 저장소 연결해 배포.

각 단계는 화면에서 클릭/붙여넣기로 됩니다. 막히면 그 단계에서 멈추고
저에게 화면(또는 에러)을 알려주세요 — 같이 해결합니다.

---

## ① Cloudflare 가입

브라우저에서 **dash.cloudflare.com** 접속 → 이메일로 회원가입/로그인.

## ② D1 데이터베이스 만들기

1. 왼쪽 메뉴 **스토리지 및 데이터베이스 → D1 SQLite 데이터베이스**.
2. **데이터베이스 생성** → 이름 `entry-pass-db` → 생성.

## ③ Database ID 저장 (이미 완료)

`wrangler.toml` 에 D1 database_id 를 넣는 단계입니다.
**이 저장소에는 이미 반영되어 있습니다.** (다른 계정에서 새로 만들 경우에만,
D1 화면 주소창의 `databases/` 뒤 문자열을 복사해 `wrangler.toml` 에 넣으면 됩니다.)

## ④ D1에 표 만들기 (스키마 적용)

1. **스토리지 및 데이터베이스 → D1 → `entry-pass-db`** 클릭.
2. 상단 **콘솔**(Console) 탭 선택.
3. GitHub 저장소의 **`schema.sql`** 전체 내용을 복사해 붙여넣고 **실행**.
   - 표(users/sessions/requests/documents)와 기본 직원 계정이 생성됩니다.
   - 한 번에 안 되면 문장 단위(`CREATE ...;`, `INSERT ...;`)로 나눠 실행.
4. **개요** 탭에서 "테이블 수"가 **4** 가 되면 성공.

## ⑤ 저장소 연결해서 배포

1. **Workers 및 Pages → 만들기(Create) → Import a repository**
   (또는 "Connect to Git" / "Workers Builds").
2. **GitHub 연결/승인** → 저장소 **dudo011/entry-pass** 선택.
3. 배포할 브랜치를 **claude/session-1-7a2uxy** 로 지정.
4. `wrangler.toml` 이 자동 인식됩니다(빌드/배포 명령 기본값 그대로,
   Deploy command 기본값 `npx wrangler deploy`).
5. **배포(Deploy)** 클릭 → 잠시 후 완료.
6. **Workers 및 Pages → entry-pass** 에서
   **`https://entry-pass.<서브도메인>.workers.dev`** 주소 확인.

## ⑥ 확인

1. 그 주소를 폰/PC 브라우저에서 열기.
2. **자재센터 직원 → `admin` / `admin1234`** 로그인 → 관리 화면이 뜨면 성공.
3. **운전기사 → 회원가입** 으로 기사 계정을 만들어 출입 신청까지 체험.

> ⚠️ 배포 후 반드시 기본 직원 비밀번호(`admin1234`, `staff1234`)를 변경하세요.
> D1 콘솔에서 변경하며, 필요할 때 방법을 안내해 드립니다.

---

## 이후 수정 반영 (피드백 루프)

⑤에서 GitHub 저장소를 연결해두면, 제가 화면·문구·안전수칙 등을 수정해
`claude/session-1-7a2uxy` 브랜치에 올리는 순간 Cloudflare가 **자동으로 다시 배포**합니다.
여러분은 배포판을 쓰며 피드백만 주시면 되고, 반영은 자동으로 이어집니다.

## 저장 용량 참고

- 서류는 업로드 시 자동 압축되어 장당 보통 수백 KB입니다.
- D1 무료 약 5GB 기준, 압축된 서류 기준 **수천~1만여 건 이상**을 3년 보관 가능.
- 용량이 부족해지면 그때 R2(파일 전용, 10GB)로 옮기는 방법을 안내드립니다.
